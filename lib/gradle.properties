group=uk.co.icatalyst
description=karyon java project library

############################
# Version declarations
############################
majorVersion=0
minorVersion=1
patchVersion=0

############################
# Dependency versions
############################
jackson_version=2.4.4
karyon_version=0.6.7
karyon_data_version=0.5.9
junit_version=4.12
coveralls_version=1.0.2
gradle_git_version=0.12.0
appcompat_version=20.0.0
robolectric_version=2.4


############################
# Source compatibility options
############################
sourceCompatibility=1.7
targetCompatibility=1.7

############################
# Build variable definitions
############################

# Default deployment to dev branch
releaseBranch=master
# Default deployment to dev branch
buildBranch=dev
# By default do not tag the build
buildTag=false
# Default repository to commit to
gitRepository=https://github.com/kmchugh/karyon.git
# Default maven branch repository
mavenBranch=mvn-repo
# Default repository to use as a maven repo
mavenRepository=https://github.com/kmchugh/karyon.git
# By default do not increment the build
incrementPatch=false
# By default do not increment the minor
incrementMinor=false
# By default this is a snapshot
isSnapshotVersion=true

############################
# Test related variables
############################

# The maximum number of tests that can be executed simultaneously
maxForks=4
# The maximum number of tests executed before a fork is recycled
maxTestsPerFork=50
# Ensure tests are enabled
testEnabled=true


